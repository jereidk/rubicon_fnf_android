# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf
# Rama de biseccion: NADA va por LFS - el presupuesto del repo esta agotado
# y `lfs: true` sin guarda tira el checkout en el paso 2.
*.pck binary
*.ctex binary
*.tar.gz binary
*.lmbake binary
precompiled_astc_imports/*.res binary
